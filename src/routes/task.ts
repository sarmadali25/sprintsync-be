import { Router } from 'express';

const router = Router();

// Public routes
router.get('/', ()=>{});
router.get('/:id', ()=>{});
router.post('/', ()=>{});
router.patch('/:id', ()=>{});
router.delete('/:id', ()=>{});



export default router;
